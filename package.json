{
  "name": "click-to-call-linphone",
  "version": "1.0.0",
  "private": true,
  "description": "Centralized outbound calling queue with just-in-time client assignment and Linphone click-to-call.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js",
    "seed": "node scripts/seed-admin.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.4.1",
    "csv-parse": "^6.1.0",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "multer": "^2.0.2"
  }
}
